{
  "name": "playlistpro-fullstack",
  "version": "1.0.0",
  "description": "PlaylistPro - Comprehensive Learning Progress Tracker",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm ci && npm run build",
    "start": "cd backend && npm start",
    "postinstall": "cd frontend && npm ci && cd ../backend && npm ci"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "rollup-plugin-visualizer": "^6.0.3"
  }
}
